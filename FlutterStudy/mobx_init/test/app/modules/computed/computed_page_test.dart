import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:mobx_init/app/modules/computed/computed_page.dart';

void main() {
  testWidgets('ComputedPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ComputedPage(title: 'Computed')));
    //  final titleFinder = find.text('Computed');
    //  expect(titleFinder, findsOneWidget);
  });
}
