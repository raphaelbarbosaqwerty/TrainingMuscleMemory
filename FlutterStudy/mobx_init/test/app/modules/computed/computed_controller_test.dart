import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:mobx_init/app/modules/computed/computed_controller.dart';
import 'package:mobx_init/app/modules/computed/computed_module.dart';

void main() {
  initModule(ComputedModule());
  // ComputedController computed;
  //
  setUp(() {
    //     computed = ComputedModule.to.get<ComputedController>();
  });

  group('ComputedController Test', () {
    //   test("First Test", () {
    //     expect(computed, isInstanceOf<ComputedController>());
    //   });

    //   test("Set Value", () {
    //     expect(computed.value, equals(0));
    //     computed.increment();
    //     expect(computed.value, equals(1));
    //   });
  });
}
