import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:mobx_formulario_reativo/app/modules/client/client_page.dart';

void main() {
  testWidgets('ClientPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ClientPage(title: 'Client')));
    //  final titleFinder = find.text('Client');
    //  expect(titleFinder, findsOneWidget);
  });
}
